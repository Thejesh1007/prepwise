{
  "name": "prepwise-backend",
  "version": "1.0.0",
  "description": "PrepWise backend API",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "npx prisma generate && npx prisma migrate deploy"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@prisma/client": "5.22.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "prisma": "5.22.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}